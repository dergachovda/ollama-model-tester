[project]
name = "ollama-model-tester"
version = "0.1.0"
description = "Minimal benchmark for local Ollama models — measures tokens/sec"
requires-python = ">=3.11"
dependencies = [
    "ollama",
    "rich",
]

